describe('PackageBuilder', () => {
  describe('Build method', () => {
    it('should give the copy folder source as src/templates/Assets');

    it('should give the copy folder destination of Assets');

    it('should send terminal answers as variable replacement option');
  });
});
